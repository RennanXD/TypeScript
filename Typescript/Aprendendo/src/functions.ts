const sum = (x:number , y:number) => {
    return x+y
}

const value = sum (2,3);

//Void e basicamente uma função que não retorna nada
const log = (message:string): void => {
    console.log(message)
}